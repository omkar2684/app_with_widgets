specVersion: "v1beta"
serviceId: "projfinale"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "projfinale-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
